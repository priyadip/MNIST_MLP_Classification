# MNIST_MLP_Classification
A neural network-based classification project using a Multi-Layer Perceptron (MLP) to recognize handwritten digits from the MNIST dataset, where input images are flattened into vectors before being fed into the model.
